---
import Base from "../layouts/Base.astro";
const base = import.meta.env.BASE_URL;
---

<Base title="Not found">
  <h1>404 — no such thing</h1>
  <p>That page doesn't exist (or its mechanism jammed).</p>
  <p><a href={base}>Back to the catalog</a></p>
</Base>
