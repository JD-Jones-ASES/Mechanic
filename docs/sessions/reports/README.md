# Phase reports

Written BY the phase-closing sessions (protocol §8), one per phase boundary, using the template in
`docs/sessions/protocol.md` §11. Audience: the owner, ~10 minutes. Nothing in this directory is
authored ahead of time.
